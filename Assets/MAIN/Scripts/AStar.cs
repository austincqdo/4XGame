using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AStar : MonoBehaviour
{

    //private Func<(float, Vector3Int), float> priorityFunction;
    //public AStar(, Func<(float, Vector3Int), float> priorityFunction)
    //{
    //    this.priorityFunction = priorityFunction;
    //}
    //// Returns a stack/some data structure with the path from start to dest using 2d a*
    //// z-axis is always 0
    //public List<(float, Vector3Int)> aStarSearch()
    //{

    //}


    //private float ManhattanDistance(Vector3Int s, Vector3Int d)
    //{
    //    return Math.abs(s.x - d.x) + Math.abs(s.y - d.y) + Math.abs(s.z - d.z);
    //}

}

